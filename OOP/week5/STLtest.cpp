#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

class Person {
private:
    string name;
    int age;
public:
    Person() {
        name = "Empty";
        age = 0;
    }
    Person(string n, int a) : name(n), age(a) {}

    int getAge()
};

bool compare(Person &p, Person &q) {
    p.get
}

int main() {

    Person p[10];

    sort(0, 3, compare);

    return 0;
}